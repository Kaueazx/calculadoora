import React from 'react';
import './Visao.css';

const Visao = ({resultado}) => {
    return (
        <div className="Visao">
            {resultado}

    

        </div>
    );
}
 
export default Visao;


// onClick = {()=>{setNum(num + 1)}}